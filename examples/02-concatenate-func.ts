// This code is to Function of Concatenate two string  

const ConcatenateStrings = (first: string, second: string): string => {
    return first + second;
}

console.log(ConcatenateStrings("Hello", "World"));